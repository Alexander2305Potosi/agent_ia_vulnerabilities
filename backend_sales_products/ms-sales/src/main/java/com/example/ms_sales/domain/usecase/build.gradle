ext {
    nettyCodecVersion = '4.1.132.Final'
}
dependencies {
    implementation project(':')
}

allprojects {
    apply from: "${rootDir}/dependencyMgmt.gradle"
    repositories { mavenCentral() }
}
